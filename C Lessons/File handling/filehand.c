#include<stdio.h>
#include<conio.h>
int main()
{
	int num;
	printf("Enter any number:");
	scanf("%d",&num);
	getch();
	if(num>0)
	{
		printf("%d is a positive number.",num);
	}
	else
	{
	
	printf("%d is a a negative number.",num);
}
	getch();
}
